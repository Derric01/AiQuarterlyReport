{
  "name": "ai-quarterly-reports",
  "version": "1.0.0",
  "description": "AI-powered quarterly equity market report generation system",
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.9.0"
  },
  "scripts": {
    "install-backend": "cd backend && python -m pip install --upgrade pip && pip install -r requirements.txt",
    "install-frontend": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/ai-quarterly-reports.git"
  },
  "keywords": [
    "ai",
    "quarterly-reports",
    "gemini",
    "financial-analysis"
  ],
  "author": "",
  "license": "MIT"
}
